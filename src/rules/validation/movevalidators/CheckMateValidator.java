package rules.validation.movevalidators;

public class CheckMateValidator {
    // After the move, the king is vulnerable? return false : return true
    /* have a list of all piece, iterate them, then for each piece of the opposite color validate -> mov currentPos kingPos*/
    protected boolean check(String request){
        return true;
    }
    protected void printError(){

    }
}
